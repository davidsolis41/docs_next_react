import React from "react";
import Paperr from "@mui/material/Paper";

function Paper() {
  return (
    <Paperr
      variant="elevation"
      component="section"
      elevation={0}
      className=""
      onClick={() => {}}
      onDoubleClick={() => {}}
      style={{}}
      sx={{}}
    ></Paperr>
  );
}

export default Paper;
