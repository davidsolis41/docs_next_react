import Box from "@mui/material/Box";
import React from "react";

function box() {
  return (
    <Box
      component="section"
      onClick={() => {}}
      onDoubleClick={() => {}}
      className=""
      style={{}}
      sx={{}}
      id=""
    ></Box>
  );
}

export default Box;
